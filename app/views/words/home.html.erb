<div class="row">
  <div class="col s8">
    <h1>10 Most Recent Words</h1>
  </div>
  <div class="col s8" id="word_list">
    <% @words.each do |word| %>
      <li><%= link_to word.word, word_path(word.id) %></li>
    <% end %>
  </div>
</div>
